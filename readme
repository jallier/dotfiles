Now using [https://www.chezmoi.io/docs/quick-start/](Chezmoi) to manage these dotfiles.

Follow the above guide to install these locally.

Note that editing the dotfiles now requires `chezmoi edit $FILE` or the changes will not be saved to this repo. To push, just run `chezmoi cd; git add .; git commit; git push`. Read the chezmoi docs if more info needed
