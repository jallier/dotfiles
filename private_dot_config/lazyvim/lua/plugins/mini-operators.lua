return {
  "echasnovski/mini.operators",
  version = false,
  event = "LazyFile",
  opts = { replace = { prefix = "gr" } },
  -- keys = {
  --    { "gr", vim.lsp.buf.references(), desc = "References", nowait = true }, -- This is for the lsp, but because it overwrites it here, add it back manually
  -- }
}
